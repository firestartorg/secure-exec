// Re-exported from @firestart/secure-exec-core — canonical source is packages/core/src/fs-helpers.ts
export type { DirEntry, StatInfo } from "@firestart/secure-exec-core";
export { exists, stat, rename, readDirWithTypes, mkdir } from "@firestart/secure-exec-core";
