{
  "name": "@nano-sandbox/ivm-bridge",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/bridge.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=iife --global-name=bridge --outfile=dist/bridge.js",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "buffer": "^6.0.3",
    "esbuild": "^0.27.1",
    "typescript": "^5.7.2"
  }
}
